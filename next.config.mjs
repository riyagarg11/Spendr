// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   /* config options here */
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ["randomuser.me"],
  // },
  eslint: {
    ignoreDuringBuilds: true,
  },
};


export default nextConfig;
